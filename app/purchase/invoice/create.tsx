import { AppShell } from '@/components/app-shell';

export default function PurchaseInvoiceCreateScreen() {
  return (
    <AppShell
      title="采购开票"
      description="后续这里接 create_purchase_invoice_from_receipt，按事实收货生成采购发票。"
    />
  );
}
