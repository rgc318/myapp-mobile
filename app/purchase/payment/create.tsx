import { AppShell } from '@/components/app-shell';

export default function PurchasePaymentCreateScreen() {
  return (
    <AppShell title="供应商付款" description="后续这里接 record_supplier_payment，负责录入实际付款。" />
  );
}
