import { AppShell } from '@/components/app-shell';

export default function PurchaseReceiptCreateScreen() {
  return (
    <AppShell title="采购收货" description="后续这里接 receive_purchase_order，支持部分收货、删行和改实际价格。" />
  );
}
