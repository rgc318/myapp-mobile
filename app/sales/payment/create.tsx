import { AppShell } from '@/components/app-shell';

export default function SalesPaymentCreateScreen() {
  return (
    <AppShell title="销售收款" description="后续这里接 update_payment_status，负责录入实收金额和后续付款信息。" />
  );
}
