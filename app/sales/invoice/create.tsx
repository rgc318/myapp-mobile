import { AppShell } from '@/components/app-shell';

export default function SalesInvoiceCreateScreen() {
  return (
    <AppShell title="销售开票" description="后续这里接 create_sales_invoice，支持部分开票和开票时改价。" />
  );
}
