import { AppShell } from '@/components/app-shell';

export default function SalesDeliveryCreateScreen() {
  return (
    <AppShell title="销售发货" description="后续这里接 submit_delivery，支持部分发货和发货时改价。" />
  );
}
